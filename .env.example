# SEC EDGAR configuration
SEC_USER_AGENT="YourName/1.0 your@email.com"
SEC_RATE_LIMIT_RPS=8

# Cache directory (defaults to .cache/ if not set)
CACHE_DIR=".cache"

# Bloomberg exports directory
BBG_EXPORT_DIR="data/bloomberg"

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL="INFO"

# Output directory
OUTPUT_DIR="out"
